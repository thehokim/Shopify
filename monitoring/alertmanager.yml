global:
  resolve_timeout: 5m
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'hokim1430@gmail.com'
  smtp_auth_username: 'hokim1430@gmail.com'
  smtp_auth_password: 'evwfxjjkzvcplucu'
  smtp_require_tls: true

route:
  receiver: 'telegram-and-email'
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  
  routes:
    # Critical alerts -> Telegram + Email
    - match:
        severity: critical
      receiver: 'critical-alerts'
      group_wait: 0s
      repeat_interval: 5m
    
    # Warning alerts -> Telegram only
    - match:
        severity: warning
      receiver: 'telegram-only'
      repeat_interval: 1h

receivers:
  # Telegram + Email для обычных алертов
  - name: 'telegram-and-email'
    telegram_configs:
      - bot_token: '8580271436:AAEEcQTRs5Xk5tjEDZq5MOPnuPNsYloSrBQ'
        chat_id: 5008138452
        parse_mode: 'HTML'
        message: |
          ⚠️ <b>Alert: {{ .GroupLabels.alertname }}</b>
          
          <b>Severity:</b> {{ .CommonLabels.severity }}
          
          {{ range .Alerts }}
          📝 {{ .Annotations.description }}
          💡 <b>Action:</b> {{ .Annotations.action }}
          {{ end }}
    
    email_configs:
      - to: 'hokim1430@gmail.com'
        headers:
          Subject: '⚠️ Alert: {{ .GroupLabels.alertname }}'
        html: |
          <h2>Alert: {{ .GroupLabels.alertname }}</h2>
          <p><strong>Severity:</strong> {{ .CommonLabels.severity }}</p>
          {{ range .Alerts }}
          <p><strong>Description:</strong> {{ .Annotations.description }}</p>
          <p><strong>Action:</strong> {{ .Annotations.action }}</p>
          <p><strong>Time:</strong> {{ .StartsAt.Format "2006-01-02 15:04:05" }}</p>
          {{ end }}

  # Только Telegram для warning
  - name: 'telegram-only'
    telegram_configs:
      - bot_token: '8580271436:AAEEcQTRs5Xk5tjEDZq5MOPnuPNsYloSrBQ'
        chat_id: 5008138452
        parse_mode: 'HTML'
        message: |
          ⚠️ <b>{{ .GroupLabels.alertname }}</b>
          
          {{ range .Alerts }}
          {{ .Annotations.description }}
          {{ end }}

  # Critical alerts -> Telegram + Email с высоким приоритетом
  - name: 'critical-alerts'
    telegram_configs:
      - bot_token: '8580271436:AAEEcQTRs5Xk5tjEDZq5MOPnuPNsYloSrBQ'
        chat_id: 5008138452
        parse_mode: 'HTML'
        message: |
          🔥🔥🔥 <b>CRITICAL ALERT</b> 🔥🔥🔥
          
          <b>{{ .GroupLabels.alertname }}</b>
          
          {{ range .Alerts }}
          🚨 {{ .Annotations.description }}
          
          ⚡ <b>URGENT ACTION REQUIRED:</b>
          {{ .Annotations.action }}
          
          🕐 Started: {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}
    
    email_configs:
      - to: 'hokim1430@gmail.com'
        headers:
          Subject: '🚨 CRITICAL: {{ .GroupLabels.alertname }}'
          Priority: 'urgent'
        html: |
          <html>
          <body style="font-family: Arial, sans-serif;">
            <div style="background-color: #ff0000; color: white; padding: 20px;">
              <h1>🔥 CRITICAL ALERT</h1>
            </div>
            <div style="padding: 20px;">
              <h2>{{ .GroupLabels.alertname }}</h2>
              <p><strong>Severity:</strong> <span style="color: red;">CRITICAL</span></p>
              {{ range .Alerts }}
              <p><strong>Description:</strong> {{ .Annotations.description }}</p>
              <p><strong>⚡ Action Required:</strong> {{ .Annotations.action }}</p>
              <p><strong>Time:</strong> {{ .StartsAt.Format "2006-01-02 15:04:05" }}</p>
              {{ end }}
            </div>
          </body>
          </html>

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'component']